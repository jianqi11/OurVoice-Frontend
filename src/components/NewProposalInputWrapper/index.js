import { styled } from "@material-ui/core";
import { Stack } from "@mui/material";

const NewProposalInputWrapper = styled(Stack)(() => ({
  flexDirection: "column",
  marginTop: 20,
}));

export default NewProposalInputWrapper;
